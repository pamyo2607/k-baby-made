{
  "name": "k-baby-made",
  "version": "1.0.0",
  "private": true,
  "description": "대한민국 제조 0~35개월 영유아 제품 공식 근거 검증 DB",
  "scripts": {
    "check": "python3 scripts/build_assets.py && python3 scripts/validate_data.py && node --check public/app.js && node --check public/kbaby-data.js",
    "verify:assets": "python3 scripts/validate_data.py && node --check public/app.js && node --check public/kbaby-data.js",
    "verify:external": "python3 scripts/validate_data.py --require-external-proof",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}
