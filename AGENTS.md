# K-Baby Made 운영 규칙

이 저장소는 대한민국에서 현재 판매되는 0~35개월 영유아 제품을 공식 근거로 검증하는 누적 데이터베이스다. 변경 시 아래 규칙을 지킨다.

- `data/master-products.json`을 canonical 제품 데이터로 사용하고 기존 레코드와 조사 이력을 임의 삭제하지 않는다.
- 원본 행 수, 고유 제품 수, 중복 연결 수를 분리한다. 중복은 삭제하지 않고 `duplicateOf`와 `canonicalProductId`로 연결한다.
- Safety Korea, 정부기관, 제조사·브랜드·수입사 공식 자료, 공식 판매처 순서로 근거를 우선한다. 검색 결과·블로그·AI 요약만으로 완료 또는 제외하지 않는다.
- OCR은 보조 수단일 뿐이다. OCR 결과만으로 KC 번호, 제조국, 월령, 제조사·수입사를 확정하지 않는다.
- KC 번호는 판매 제품과 인증 모델의 브랜드, 제품명, 모델명, 제조사, 외형·품목·파생모델 중 최소 두 가지 이상이 일치할 때만 연결한다. 다른 옵션·세트 구성품·공통 상세의 번호를 재사용하지 않는다.
- 모든 제품에 어린이제품 KC 번호를 강요하지 않는다. 완구·어린이제품, 위생용품, 화장품, 식품 접촉 제품, 전기용품의 적용 법령을 제품별로 나눈다.
- 공식 근거가 부족하면 `보류`다. 판매 종료, 해외 직구 전용, 36개월 이상 전용, 대한민국 완제품 제조 기준 미충족, 중복 등 제외 사유도 공식 근거가 있어야 한다.
- 자동 조사에서는 기존 보류 제품 최대 50개와 신규 후보 최대 30개만 다룬다. 새 후보는 exact product identity를 확인하기 전 canonical DB에 넣지 않는다.
- 빌드와 검증이 모두 통과하지 않으면 생성물을 커밋하거나 배포하지 않는다. 네트워크 일시 오류를 데이터 판정으로 바꾸지 않는다.
- 배포 뒤 운영 정적 파일의 SHA-256과 Chromium 초기화, 24개 카드, 검색·필터·상세·KC 필드·공식 링크를 실제로 검증한다.
- `.env`, API key, 토큰, cookie, session, credential, authorization header를 저장소·proof·로그에 기록하지 않는다.

기본 검증 명령은 `npm run check`다. 배포 전 `npm run verify:assets`와 `npx wrangler deploy --dry-run`도 실행한다.
